# 在线镜像/实例规格清单（可选，构建时注入，运行时自动拉取）
# 返回结构：{ "images": { "aliyun": [{ "value": "...", "label": "..." }] }, "instanceTypes": { ... } }
# 支持仅包含部分厂商，未提供的厂商使用本地内置清单
VITE_CATALOG_URL=

# 云厂商 API 代理（可选）。浏览器直连厂商 API 需处理签名（AK/SK）与 CORS，建议由后端代理后返回规范化结果。
# 本仓库内置代理见 server/（Go + Gin，密钥与环境变量配置方式见 server/README.md）。
# 方式一（完整清单）：填写一个返回上述结构的地址
# 方式二（按需请求）：URL 使用 {vendor} / {kind} 占位符，kind 为 images 或 instanceTypes，
#   例如 http://localhost:8080/api/{kind}/{vendor}
#   响应为 { "items": [{ "value": "...", "label": "...", "zones": ["ap-guangzhou-5"] }] }
# 注：按地域拉取需在 URL 中加入 {region}（前端支持后接入，例如 /api/{kind}/{vendor}/{region}）
VITE_CATALOG_API_URL=
